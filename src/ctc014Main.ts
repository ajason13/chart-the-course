import {
  analyzePdf,
  renderJsPdfDirect,
  renderJsPdfSvg,
  renderPdfKitDirect,
  renderPdfKitSvg,
} from "./ctc014Experiment";

window.ctc014Experiment = {
  analyzePdf,
  renderJsPdfDirect,
  renderJsPdfSvg,
  renderPdfKitDirect,
  renderPdfKitSvg,
};
